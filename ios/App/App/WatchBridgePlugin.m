#import <Foundation/Foundation.h>
#import <Capacitor/Capacitor.h>

// Register the plugin so Capacitor can find it by name "WatchBridge"
CAP_PLUGIN(WatchBridgePlugin, "WatchBridge",
    // No callable methods — this plugin only fires events
)
