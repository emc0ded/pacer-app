# Pacer

## Design System
Follow the style guide at `pacer-style-guide.md` for all UI work.
Fonts: Bebas Neue (display), IBM Plex Mono (labels/UI), DM Sans (body).
Themes: dark (default) and light — always use CSS custom properties, never raw hex values.